{
  "name": "alpha-coin-report-auto",
  "version": "1.0.0",
  "description": "Alpha Coin Report - daily automated crypto mover reports with 11ty and Netlify",
  "scripts": {
    "start": "npx @11ty/eleventy --serve --input=src --output=_site",
    "build": "npx @11ty/eleventy --input=src --output=_site",
    "generate": "node scripts/generate-report.js"
  },
  "dependencies": {
  "node-fetch": "^3.3.2"
},
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}
